#include <stdio.h>
int factorial(int n)
{
    int fact=1;
    for(int i=1;i<=n;i++)
    {
        fact=fact*i;
    }
    if (n==0||n<0)
    fact=0;
    return fact;
}
void main()
{
   int n;
   printf("Enter a number:");
   scanf("%d",&n);
   int ans=factorial(n);
   printf("factorial=%d",ans);
}
